{
  "name": "@notifykit/demo-server",
  "version": "0.1.0",
  "private": true,
  "description": "Demo: notify() fan-out with console channels + the standard notification endpoints (x-user header stands in for auth).",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "tsx server.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@notifykit/core": "0.1.0",
    "@notifykit/express": "0.1.0",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "tsx": "^4.19.2"
  }
}
